struct Animal {
    var name = "Cow";
    var age: Int;

    init (age: Int) {
      self.age = age;
    }

    func age() {
      age = age + 1;
    }
}

var cow = Animal(age: 4);

print(cow.age);
cow.age();
print(cow.age);
