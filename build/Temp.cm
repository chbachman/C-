class String {
	val buffer: Int
	var length = 24

	init {
		buffer = length
	}

	fun append(s: String) {
		length = length + s.length
	}
}

fun main(): Int {
	val temp = String()
	val temp2 = String()

	temp.append(temp2)

	print(temp.length)
}
